--- /dev/null
+++ b/finalforeach/cosmicreach/settings/IntSetting.java
@@ -1,0 +_,39 @@
+package finalforeach.cosmicreach.settings;
+
+public class IntSetting extends GameSetting implements INumberSetting {
+   private final String key;
+   private int value;
+
+   public IntSetting(String key, int defaultValue) {
+      this.key = key;
+      if (ALL_SETTINGS.getOrDefault(key, defaultValue) instanceof Number n) {
+         this.value = n.intValue();
+      } else {
+         this.value = defaultValue;
+      }
+   }
+
+   public int getValue() {
+      return this.value;
+   }
+
+   private void save() {
+      ALL_SETTINGS.put(this.key, this.value);
+      saveSettings();
+   }
+
+   public void setValue(int newValue) {
+      this.value = newValue;
+      this.save();
+   }
+
+   @Override
+   public float getValueAsFloat() {
+      return this.getValue();
+   }
+
+   @Override
+   public void setValue(float newValue) {
+      this.setValue((int)newValue);
+   }
+}
